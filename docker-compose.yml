version: "3.9"

services:
  backend:
    build: .
    ports:
      - "8001:8000"
    depends_on:
      - mongodb
      - redis

  mongodb:
    image: mongo
    ports:
      - "27017:27017"

  redis:
    image: redis
    ports:
      - "6379:6379"